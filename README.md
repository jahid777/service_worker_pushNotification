# service_worker_pushNotification

backend:
*npm install -g web-push
*web-push generate-vapid-keys (generate the key)

frontend:
*service worker file have to make in the public folder
*then Initialize it in the app.js

